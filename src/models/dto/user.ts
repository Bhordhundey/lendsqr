export interface IUser {
    firstName: string;
    lastName: string;
    email: string
}

export interface UserModel {
    id?: number;
    first_name: string;
    last_name: string;
    email: string
}